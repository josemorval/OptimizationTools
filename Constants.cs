using UnityEngine;
using System.Collections;

namespace OptimizationTools
{
  public static class Constants {
    public static class Modes
    {
      public const string Debug = "OT_DEBUG_INFO";
      public const string Safe = "OT_SAFE_MODE";
      public const string PoolAutomaticRelease = "OT_POOL_AUTOMATIC_RELEASE";
    }
  }
}
